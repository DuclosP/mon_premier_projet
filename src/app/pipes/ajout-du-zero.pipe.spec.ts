import { AjoutDuZeroPipe } from './ajout-du-zero.pipe';

describe('AjoutDuZeroPipe', () => {
  it('create an instance', () => {
    const pipe = new AjoutDuZeroPipe();
    expect(pipe).toBeTruthy();
  });
});
