import { SecundsLeftPipe } from './secunds-left.pipe';

describe('SecundsLeftPipe', () => {
  it('create an instance', () => {
    const pipe = new SecundsLeftPipe();
    expect(pipe).toBeTruthy();
  });
});
