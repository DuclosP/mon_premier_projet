import { ToFahreneitPipe } from './to-fahreneit.pipe';

describe('ToFahreneitPipe', () => {
  it('create an instance', () => {
    const pipe = new ToFahreneitPipe();
    expect(pipe).toBeTruthy();
  });
});
